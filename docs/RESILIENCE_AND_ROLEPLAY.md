# الصمود ومحاكاة الأدوار — 2026-08-20

## السؤال: هل يشتغل لو طالب لصق نصه فقط؟

نعم — الآن في كل الحالات:

- **مع مزوّد AI مُهيّأ (الوضع الأساسي):** يُحلّل النص عبر بوابة الذكاء الاصطناعي (`prompts.ts` + `ai.ts`) بجودة احترافية عبر كل المواد. هذا هو المسار المقصود للإنتاج.
- **بلا مزوّد AI (شبكة الأمان الجديدة):** بدل رفض الطالب بخطأ `AI_NOT_CONFIGURED`، يعمل **المُجمِّع المحلي الحتمي** `src/server/native-compiler.ts` وينتج مشروعًا قابلًا للعمل من النص (عنوان، إجراءات، مخرجات، Rubric بالنسب، موعد، نمط استشهاد، مجال أكاديمي) — **موسومًا بوضوح `needs_confirmation` وبعلم صريح في `riskFlags`**، دون تزييف فهم الـAI. الاستجابة تحمل `compiler: "native_fallback"` و`notice`.

### السبب الجذري الذي عولج
مسار `/api/projects/compile` كان يستدعي `getAIProvider().compileAssignment()` دائمًا بلا بديل؛ أي غياب/انقطاع للمزوّد = جدار للطالب. أُضيف تفرّع: `if (!aiConfigured && نص فقط) → native` (الملفات متعددة الوسائط تبقى تتطلب نموذجًا، وهذا صحيح).

## محاكاة الأدوار (`tests/roleplay.test.ts`)

لعبنا الأدوار الثلاثة فعليًا بمدخلات متوقعة وعدائية — **6/6 ناجحة**:

- **الطالب** عبر مواد: هندسة، قانون، طب، حاسوب، إحصاء، أدب، عرض تقديمي → كلها تُجمَّع لمشروع سليم، وتُكتشف الإشارات الخاصة (موعد «خلال 14 يوم»، نسب Rubric في سطر واحد، IEEE/MLA/APA، الوضع الجماعي، المجال الأكاديمي).
- **الطالب (غير المتوقع):** نص فارغ، حرف واحد، إيموجي، 300 ألف حرف، محاولة حقن تعليمات، HTML/`<script>`، نسبة 200% غير صالحة، تاريخ 99/99/9999، تلويث `__proto__` → **لا انهيار**، النسب مقصوصة، الإجراءات تعود للافتراضي، ولا تلوّث prototype.
- **المعلم:** مُدقّق نزاهة التصميم يميّز التكليف الأصيل عن الاسترجاعي؛ معايرة العدالة تكشف المتساهل/المتشدد.
- **الجامعة:** ملف الاعتماد + مؤشر الازدحام + الرسم الفيدرالي يتركّبون بلا خطأ وبأرقام مشتقة من بيانات حقيقية.

## هل هذا «الكمال المطلق»؟

لا يوجد برمجية بلا عيوب، لكن الوضع الآن:
- **الأنبوب الكامل مُحصّن ضد المدخلات المتوقعة وغير المتوقعة** (مثبت باختبار، لا ادّعاء).
- **الجودة الدلالية عبر كل المواد** تعتمد على مزوّد الـAI — وهذا صحيح لمنتج ذكاء اصطناعي؛ شبكة الأمان تضمن ألا ينكسر التدفّق أبدًا.
- ما يزال خارج الكود: بوابات الإطلاق السبع (اعتماد، بنية، اختبار بشري) في `GO_LIVE_RUNBOOK.md`.

### التحقق
`typecheck` 0 أخطاء · المجموعة الكاملة **33/33 ناجحة** (منها 6 محاكاة أدوار) · `npm run build` ناجح.
