# SECURITY-TODO

قائمة ملاحظات أمنية وتحسينات **لم تُطبَّق** في جولة التنظيف الآمن لأنها قد تكسر التشغيل أو تحتاج
اختباراً/موافقة المالك. لم يُغيَّر أي منطق تشغيل أو مصادقة أو قواعد Firestore/Storage أو CSP في هذه الجولة.

## أولاً: ملاحظات أمنية تحتاج مراجعة المالك (بلا تنفيذ)

1. **CSP يسمح بـ `unsafe-inline` و`unsafe-eval`.**
   - الموقع: `server.ts:1845-1855` (سلسلة `script-src` و`style-src`).
   - `script-src` يحتوي `'unsafe-inline' 'unsafe-eval'` في الإنتاج، و`style-src 'unsafe-inline'`.
   - يضعف الحماية من XSS. تشديده يحتاج nonce/hash للسكربتات المضمّنة واختبار رسم كامل — مؤجَّل.

2. **صلاحية superadmin تُمنح بالبريد عبر متغيّرات البيئة.**
   - الموقع: `server.ts:362-372` (`PLATFORM_OWNER_EMAILS` من `ROOT_OWNER_EMAIL` و`SUPERADMIN_EMAILS`) و`server.ts:568`
     (`isPlatformOwnerEmail(emailLower) ? "superadmin" : "student"`).
   - أي بريد مطابق في env يحصل على أعلى صلاحية. يحتاج ضبط دقيق للبيئة الإنتاجية ومراجعة من يملك تعديل env.

3. **`CHECK_REVOKED_ID_TOKENS=false` افتراضياً.**
   - الموقع: `server.ts:539-540` (`const checkRevoked = process.env.CHECK_REVOKED_ID_TOKENS === "true"`).
   - عندما تكون false لا يتحقّق `verifyIdToken` من إبطال التوكن (جولة إضافية للـAuth backend).
   - يعني أن التوكنات المُبطَلة قد تبقى صالحة حتى انتهائها. تفعيلها له كلفة أداء — قرار المالك.

4. **سرّ `variation` مضمّن كقيمة احتياطية داخل الكود.**
   - الموقع: `src/server/project-writer.ts:359-361`.
   - القيمة الاحتياطية: `"academicos-development-variation-v1"` عند غياب `PROJECT_VARIATION_SECRET`
     و`CSRF_SIGNING_SECRET`.
   - سرّ توقيع ثابت في المصدر. يجب ضمان ضبط المتغيّر في الإنتاج وعدم الاعتماد على القيمة المضمّنة.

5. **غياب التحقّق من نوع الملف (magic bytes) عند الرفع.**
   - لم يُعثر على تحقّق من توقيع البايتات (magic bytes) لملفات الرفع؛ الاعتماد يبدو على الامتداد/الـ
     content-type المُرسَل من العميل، وهو قابل للتزوير. يُنصح بالتحقّق من التوقيع فعلياً قبل القبول/التخزين.

6. **مفتاح Firebase مشحون في المستودع مقابل ادعاء LAUNCH_REPORT.**
   - الملف: `firebase-applet-config.json` يحتوي `projectId: academicos-3991f` و`apiKey: AIzaSyCKkJ_...`
     و`appId` و`oAuthClientId` لمشروع Firebase حقيقي.
   - بينما يذكر `LAUNCH_REPORT.md` (منقول إلى `docs/changelog/`) صراحةً: "Firebase runtime config is
     deployment-injected; no developer Firebase project is shipped in the patch."
   - هذا تناقض: مشروع مطوّر Firebase **مشحون فعلاً** في المستودع. مفاتيح Firebase الويب ليست سرّاً بحد
     ذاتها (معرّفات عامة)، لكن يجب تأكيد أن قواعد Firestore/Storage (deny-all حالياً) وApp Check تحمي المشروع،
     ومراجعة ما إذا كان يجب إزالة الملف والاعتماد على الحقن وقت النشر كما يدّعي التقرير. لم يُحذف (قد يُستخدم في البناء).

7. **"الأدلة" إقرار ذاتي، والصندوق الأسود غير موصول.**
   - آلية "الأدلة/جواز التأليف" تعتمد على إقرار ذاتي من المستخدم، لا على قياس مستقل موثوق.
   - مسجّل الصندوق الأسود على العميل (`src/lib/black-box-recorder.ts`) كان **غير موصول** بأي واجهة
     (صفر مستوردين؛ إشارة واحدة في تعليق داخل `src/server/advanced/learning-black-box.ts`). حُذف الملف في هذه
     الجولة كـ«كود ميت». التحقّق الخادمي `verifyBlackBoxChain` موجود ومُختبَر في `tests/advanced.test.ts` لكنه
     يعمل على سلسلة يرسلها العميل طوعاً، فلا يوجد تسجيل فعلي موصول. أي إعادة تفعيل لهذه الميزة تحتاج إعادة كتابة
     المسجّل وربطه، مع مراجعة الخصوصية والموافقة.

## ثانياً: تحسينات أداء مؤجَّلة (تحتاج اختباراً)

8. **تحميل i18n الثقيل استاتيكياً.**
   - `src/lib/i18n-messages.ts` بحجم ~1.08MB يُستورد استاتيكياً في `src/lib/i18n.tsx:2`
     (`import { MESSAGES } from "./i18n-messages";`).
   - يثقل حزمة أول رسم. **لم يُحوَّل إلى تحميل ديناميكي** لأن ذلك قد يكسر أول رسم/الـSSR — يحتاج اختباراً
     لاحقاً (تقسيم حسب اللغة أو تحميل كسول مع fallback متزامن للّغة الافتراضية).

## ثالثاً: تنظيف تاريخ git (يحتاج موافقة المالك)

9. لم يُعاد كتابة تاريخ git في هذه الجولة (لا rebase/amend/force-push). حذف الملفات من الشجرة يزيلها من
   الحالة الحالية فقط؛ تبقى في التاريخ. إن احتوى التاريخ على أي أسرار/PII، فتنظيفه (git filter-repo أو ما شابه)
   يحتاج موافقة منفصلة من المالك. لم تُحذف في هذه الجولة أي ملفات PII (لم يُعثر عليها ضمن النطاق المُنفَّذ).
