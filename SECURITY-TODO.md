# SECURITY-TODO

سجلّ الملاحظات الأمنية: ما **أُنجز** وما **بقي**. البنود المتبقّية إمّا تحتاج موافقة المالك
وإمّا تحتاج تحقّقًا في متصفّح حيّ لا يمكن إجراؤه في بيئة CI.

---

## أولاً: ما أُنجز

### ✅ 1. تفعيل التحقّق من إبطال الرموز (`CHECK_REVOKED_ID_TOKENS`)

- **الملفات:** `server.ts` (`shouldCheckRevokedIdTokens` و`verifyFirebaseIdToken`)، `.env.example`،
  `scripts/deploy-cloud-run.sh`.
- صار التحقّق **مفعّلًا افتراضيًا عندما `NODE_ENV=production`**، فلا تبقى جلسة مُبطَلة صالحة حتى انتهاء
  صلاحيتها. المتغيّر `CHECK_REVOKED_ID_TOKENS` ما زال يتجاوز الافتراضي في الاتجاهين (`"false"` لتعطيله
  في التطوير والمحاكيات).
- **معالجة خطر التوقّف الموثّق سابقًا:** إن كان حساب خدمة Cloud Run يفتقر لصلاحية Firebase Auth الإضافية،
  كان تفعيل الخيار سيرفض *كل* رمز صالح. لذلك: الرمز المُبطَل/المعطَّل يُرفض دائمًا
  (`auth/id-token-revoked`, `auth/session-cookie-revoked`, `auth/user-disabled`, `auth/user-not-found`)،
  ولا يُلجأ للتحقّق المحلّي إلا عند فشل بنيوي في الجولة الإضافية، مع تسجيل خطأ مرّة واحدة يطلب منح
  الصلاحية. أي أن الفشل «مغلق» أمنيًا و«مفتوح» تشغيليًا فقط لأعطال البنية.
- سكربت النشر صار يضبط `CHECK_REVOKED_ID_TOKENS=true` بدل `false`.

### ✅ 2. إزالة كل سرّ حرفي من الكود

- **الملفات:** `src/server/variation-secret.ts` (جديد)، `src/server/project-writer.ts`،
  `src/server/solver.ts`، `src/server/abuse-guard.ts`.
- حُذفت القيم الاحتياطية المحفورة `"academicos-development-variation-v1"`،
  `"academicos-development-solve-v1"` و`"academicos-development-only-abuse-secret"`.
- `resolveVariationSecret(domain)` يقرأ `PROJECT_VARIATION_SECRET` ثم `CSRF_SIGNING_SECRET`، ويحتفظ بفصل
  النطاقات (project/solve) عبر بادئة. **السلوك الآمن عند الغياب:** سرّ عشوائي لكل عملية تشغيل مع تحذير
  واحد في السجل — لا يُخمَّن ولا يُشحن في المستودع، والثمن الوحيد أن معرّفات التنويع تفقد ثباتها عبر
  إعادة التشغيل. الإنتاج ما زال يفرض ضبط المتغيّر عبر `scripts/verify-production-env.mjs`
  (٣٢ محرفًا على الأقل).
- `abuse-guard` يظلّ يرمي خطأ في الإنتاج عند غياب `ABUSE_HASH_SECRET`؛ والاحتياطي في التطوير صار عشوائيًا.

### ✅ 3. التحقّق من نوع الملف عبر التوقيع الثنائي (magic bytes)

- **الملفات:** `src/server/file-extract.ts` (`assertSupportedFileContent`)، `server.ts` (`validateFile`).
- لم يعد `mimeType` ولا الامتداد المُرسَلان من العميل كافيين: تُقرأ البايتات الأولى فعليًا ويُقارَن نوعها
  بما ادّعاه العميل.
- **الأنواع المسموحة** مقصورة على ما يستهلكه التطبيق حقًّا (حسب `accept=` في الواجهة و`extractFileText`):
  PDF، صور نقطية (PNG/JPEG/GIF/WebP/BMP/TIFF/HEIC/AVIF/ICO)، مستندات OOXML/ZIP (docx/pptx/xlsx/odt/zip)،
  ونصوص. **الصوت والفيديو استُبعدا** لأن أيّ مسار رفع في الواجهة لا يطلبهما.
- التحقّق يجري داخل `validateFile`، فيغطّي مساري الرفع القائمين (`/api/learn/intake` وتجميع التكليف) دون
  تغيير في أيٍّ منهما. مسار WhatsApp لا يمرّ بـ`validateFile` (وسائطه تُنزَّل من WhatsApp لا من العميل).
- تسامح مقصود مع سلوك المتصفّحات: يُقبل الملف إذا طابق توقيعُه **إمّا** الـmimeType **أو** الامتداد، حتى لا
  ينكسر رفع `.docx`/`.md` حين يرسل المتصفّح `application/octet-stream`.
- مغطّى باختبارات انحدار في `tests/security-regressions.test.ts` (قبول المسارات الحقيقية، ورفض ELF باسم
  `.png`، وHTML يدّعي `image/png`، وZIP يدّعي PDF، والامتدادات غير المدعومة، والملف الفارغ).

### ✅ 4. تقييد ترقية superadmin بالبريد

- **الملفات:** `server.ts` (`isPlatformOwnerEmail`، `authenticate`، `userRole`)، `.env.example`.
- **تصحيح للسجل:** القائمة كانت **أصلًا** من متغيّرات البيئة (`ROOT_OWNER_EMAIL` و`SUPERADMIN_EMAILS`)
  ولم تكن محفورة في الكود، وتوجد آلية custom claims فعلية (`scripts/bootstrap-root.mjs`).
- ما أُصلح فعليًا ثغرتان:
  1. **`userRole()` كان يتجاوز الـcustom claims**: أي بريد في القائمة يُعاد كـ`superadmin` مهما كان الـclaim،
     فكان خفض الصلاحية إداريًا لا يُطبَّق في قوائم المستخدمين، بل وكان **يُنزّل** `root_owner` إلى
     `superadmin`. صار الـclaim الصريح هو المرجع، والبريد احتياطيًا فقط — متوافقًا مع ما يفرضه
     `authenticate` أصلًا.
  2. **لم يكن يُشترط توثيق البريد**: صار `isPlatformOwnerEmail` يتطلّب `emailVerified`، فلا يكفي تسجيل
     حساب ببريد مطابق للحصول على أعلى صلاحية.
- لم تُعَد هندسة المصادقة: لا تغيير في تدفّق تسجيل الدخول ولا في الأدوار ولا في `firestore.rules`.

### ✅ 6 (جزئيًا). تشديد CSP بما لا يكسر الموقع

- **الملفات:** `server.ts` (رأس `Content-Security-Policy`)، `.env.example`.
- **أُضيفت `script-src-attr 'none'`**: تمنع كل معالِجات الأحداث المضمّنة (`onclick=`, `onerror=` …) وهي
  فئة كاملة من هجمات XSS المحقونة، دون المساس بكتل `<script>` المضمّنة التي يحتاجها `index.html`.
  تحقّقنا أنه لا وجود لأي سمة `on*=` في `index.html` ولا في ناتج البناء، والاختبار يحرس ذلك.
- **`CSP_ALLOW_UNSAFE_EVAL`**: متغيّر جديد يسمح بإسقاط `'unsafe-eval'` دون تعديل الكود. الافتراضي يُبقي
  السلوك الحالي كما هو تمامًا.

---

## ثانيًا: ما بقي ولماذا

### ⏸️ 5. `unsafe-inline` في `script-src` و`style-src` — **تُركت عمدًا**

ليست قابلة للإزالة اليوم؛ الاعتماد عليها **موثَّق بالأدلة** لا بالحدس:

- `index.html` فيه **كتلتا `<script>` مضمّنتان**: حارس وميض السمة (theme flash) وشاشة الإقلاع
  (`acos-booting`). إزالة `'unsafe-inline'` من `script-src` تُعطّلهما فتُترك شاشة الإقلاع معلّقة.
- `index.html` فيه **كتلة `<style>` مضمّنة**، وفي الواجهة **٤٠ موضع `style={{…}}`** في JSX. سمات النمط
  المضمّنة تخضع لـ`style-src-attr` الذي يرث `style-src`، فإزالتها تكسر التنسيق.
- **الطريق الصحيح لاحقًا:** nonce أو hash يُحقن وقت البناء لكتل `index.html` الثلاث، مع الانتباه إلى أن
  خادم التطوير (Vite في وضع middleware) يحقن سكربتات مضمّنة خاصة به تحتاج معالجة منفصلة. يحتاج تحقّقًا
  بصريًا في متصفّح.

### ⏸️ 6. `unsafe-eval` — **تُركت مفعّلة افتراضيًا (قابلة للإطفاء بمتغيّر)**

- **حزمُنا نظيفة:** فُحصت كل ملفات البناء الـ٤٦ في `dist/assets/*.js` فلم يُعثر على `eval(` ولا
  `new Function` ولا مرّة واحدة.
- **المستهلك الوحيد المتبقّي طرف ثالث:** Google reCAPTCHA Enterprise، وهي أساس **Firebase App Check**
  (`src/lib/firebase.ts`) و`RecaptchaVerifier` في `src/pages/Login.tsx`. مصادر متعدّدة توثّق أن بعض إصدارات
  reCAPTCHA تستخدم تقييمًا ديناميكيًا وتتطلّب `'unsafe-eval'`.
- تعذّر التحقّق: لا متصفّح في البيئة، ووكيل الشبكة يحجب `www.google.com` فلم نستطع فحص سكربت reCAPTCHA.
- **الخطر لو أُزيلت خطأً:** يفشل الحصول على رمز App Check، فتُرفض **كل** طلبات الـAPI (ترويسة
  `X-Firebase-AppCheck`) وينكسر تسجيل الدخول — أي تعطيل كامل للموقع الحيّ.
- **الخطوة التالية للمالك:** انشر بـ`CSP_ALLOW_UNSAFE_EVAL=false`، وافتح صفحة الدخول ووحدة تحكّم المتصفّح،
  وتأكّد من عدم ظهور `EvalError`/`Refused to evaluate a string` ومن نجاح تسجيل الدخول وطلبات الـAPI.
  إن نجح فاجعلها دائمة.

### ⏸️ 8. تحميل i18n الثقيل (~1.08MB) استاتيكيًا — **تُرك، غير قابل للتحقّق هنا**

- `src/lib/i18n-messages.ts` (~1.08MB) يُستورد استاتيكيًا في `src/lib/i18n.tsx:2`، فيحمل المستخدم اللغات
  الثماني كلها ليستعمل واحدة.
- **سبب الترك:** التحويل لتحميل ديناميكي يتطلّب تقسيم الرسائل حسب اللغة، والملف **مقترن بأداتين** تقرآنه
  نصيًا: `scripts/global-ui-audit.cjs` يُحلّل كائن `MESSAGES` عبر TypeScript AST ويتحقّق من شكل اللغات،
  و`scripts/static-release-audit.mjs` يقرأ الملف. إعادة هيكلته تكسر `npm run audit:launch`.
- وقاعدة «لا كود أحمر» حاسمة هنا: عطبٌ في التقسيم يجعل كل نصوص الواجهة تُعرض كمفاتيح خام
  (`layout.navPlatformShort`) أو يُظهر شاشة فارغة، ولا يوجد متصفّح في البيئة للتحقّق البصري.
- **مقدّمات مواتية رُصدت لمن ينفّذها لاحقًا:** اللغة معروفة **قبل** تحميل وحدة التطبيق عبر
  `public/locale-bootstrap.js` (يضبط `documentElement.dataset.locale`)، وهناك شاشة إقلاع تغطّي الشاشة حتى
  أول رسم (`__acosBootReady`). فيمكن تحميل رسائل اللغة الأولى **قبل** `createRoot().render()` في
  `src/main.tsx` دون أي وميض للمفاتيح.
- **خطة مقترحة:** إبقاء `i18n-messages.ts` مصدرًا للتأليف (فلا تنكسر أداتا التدقيق)، وتوليد وحدة لكل لغة
  منه (سكربت مُلزَم باختبار يُعيد التوليد ويقارن، فيمنع الانحراف)، ثم `import()` ديناميكي للّغة المطلوبة
  وحدها، و`setLocale` يحمّل قبل التبديل. المكسب المتوقّع: ~⅞ من حمولة i18n في أول رسم.

### ⏸️ 6 (الأصلي). مفتاح Firebase مشحون في المستودع مقابل ادعاء LAUNCH_REPORT — لم يُغيَّر

- `firebase-applet-config.json` يحتوي `projectId` و`apiKey` و`appId` لمشروع Firebase حقيقي، بينما يذكر
  `LAUNCH_REPORT.md` (في `docs/changelog/`) أن الإعداد يُحقن وقت النشر. مفاتيح Firebase للويب معرّفات عامة
  لا أسرار، لكن يجب تأكيد أن قواعد Firestore/Storage وApp Check تحمي المشروع. لم يُحذف الملف لأن
  `scripts/bootstrap-root.mjs` وغيره يقرؤونه.

### ⏸️ 7. «الأدلة» إقرار ذاتي، والصندوق الأسود غير موصول — لم يُغيَّر

- آلية «الأدلة/جواز التأليف» تعتمد إقرارًا ذاتيًا لا قياسًا مستقلًّا. `verifyBlackBoxChain` موجود ومُختبَر في
  `tests/advanced.test.ts` لكنه يعمل على سلسلة يرسلها العميل طوعًا، ومسجّل العميل حُذف سابقًا ككود ميت. أي
  إعادة تفعيل تحتاج إعادة كتابة المسجّل وربطه مع مراجعة الخصوصية والموافقة.

### ⏸️ 9. تنظيف تاريخ git — يحتاج موافقة المالك

- لم يُعَد كتابة التاريخ (لا rebase/amend/force-push على `main`). حذف ملف من الشجرة يزيله من الحالة الحالية
  فقط ويبقى في التاريخ. إن احتوى التاريخ على أسرار/PII فتنظيفه (`git filter-repo`) يحتاج موافقة منفصلة.
